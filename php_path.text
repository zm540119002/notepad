
==========================================================================================================================
//入口文件目录
$path = dirname($_SERVER['SCRIPT_FILENAME']);
//入口文件上级目录
if (is_file('./think')) {
	$rootPath = realpath($path) . '/';
} else {
	$rootPath = realpath($path . '/../') . '/';
}
==========================================================================================================================